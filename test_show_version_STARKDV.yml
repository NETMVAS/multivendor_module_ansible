- name: "change_hostname"
  hosts: "{{ hosts }}"
  gather_facts: true
  ignore_errors: true
  tasks:

    - name: show interface
      routeros_facts:
        gather_subset: "{{ subset }}"

    - name: Debug config
      debug: var=ansible_net_model
