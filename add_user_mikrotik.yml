- name: "change_hostname"
  hosts:
    - AP
    - BRAS
    - SWITCH
  gather_facts: false
  ignore_errors: true
  roles:
      - role: config_user
        username: "{{ username }}"
        password: "{{ password }}"
        group: "{{ group }}"
        action: "{{ action }}"
